async function placeOrder(alpaca, orderType, symbol, side, quantity, price) {
  // Implement your order placing logic using Alpaca's API here
}

async function cancelOrder(alpaca, id) {
  // Implement your order canceling logic using Alpaca's API here
}

module.exports = {
  placeOrder,
  cancelOrder,
};
